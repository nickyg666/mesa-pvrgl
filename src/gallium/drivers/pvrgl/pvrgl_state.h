/* pvrgl_state.h — state tracker function tables init. */
#ifndef PVRGL_STATE_H
#define PVRGL_STATE_H

struct pipe_context;

void
pvrgl_context_init_state_functions(struct pipe_context *pctx);

#endif /* PVRGL_STATE_H */
